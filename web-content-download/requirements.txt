# Scrapling Web Content Extractor Dependencies
# Install with: pip install -r requirements.txt

git+https://github.com/D4Vinci/Scrapling.git
html2text>=2020.1.16
requests>=2.28.0  # For image downloading

# Optional: if git install fails, install dependencies manually:
# pip install lxml cssselect orjson tld w3lib typing_extensions
# pip install curl_cffi playwright browserforge msgspec anyio click
